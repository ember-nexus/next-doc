# Project-specific dictionary for cspell.
# One word per line. Lines starting with # are comments.

# Ember Nexus / project vocabulary
hypergraph
backlinks

# placeholder marker used on unwritten stub pages (not a real word — see
# guide/*.mdx pages whose entire body is just "blub"; whitelisted so the
# spellcheck stays green, but those pages still need real content)
blub

# WebDAV / HTTP methods and specs
PROPFIND
PROPPATCH
MKCOL
httpbis

# tools & libraries
antv
dagre
healthcheck
Symfony

# valid English words missing from the base dictionary
explorative
Geospatial
lilytree

# markdown-bold parsing artifacts (e.g. "**c**reate, **r**ead, **u**pdate, **d**elete")
reate
pdate
elete
