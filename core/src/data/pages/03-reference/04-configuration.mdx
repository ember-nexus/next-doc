---
type: reference
title: Configuration
---

Ember Nexus API can be configured through environment variables (database connections, common options) and by providing
a YML config file for lesser used settings.

A refactoring of the configuration process is planned in [issue #463](https://github.com/ember-nexus/api/issues/463).

## Environment variables

Ember Nexus specific variables:

- `ANONYMOUS_USER_UUID`: The UUID of a user, which will be used if no token is supplied.
- `CYPHER_AUTH`: Connection string for accessing the OpenCypher database, i.e. Neo4j.
- `MONGO_AUTH`: Connection string for accessing the Mongo database.
- `ELASTIC_AUTH`: Connection string for accessing the Elasticsearch database.
- `REDIS_AUTH`: Connection string for accessing the Redis database.
- `RABBITMQ_AUTH`: Connection string for accessing the RabbitMQ database.
- `VERSION`: Variable containing the version of the currently running API software. Automatically set by the build
process.
- `REFERENCE_DATASET_VERSION`: Variable containing the version of the
[reference dataset](https://github.com/ember-nexus/reference-dataset) used for validating features.
- `DISABLE_CRON`: Set to `true` to disable the [background tasks](#background-tasks) every API instance would
otherwise run automatically. Defaults to `false`.

Framework specific variables:

See [Symfony's documentation](https://symfony.com/doc/current/configuration.html#configuration-based-on-environment-variables)
for details.

## Background tasks

Every API container instance runs the [`cron`](/command/cron) command automatically, every 5 minutes, for as long as
the container is up - there is nothing to schedule externally, and running multiple instances in parallel is safe
since the individual tasks (e.g. [`cron:reindex-files`](/command/cron-reindex-files)) consume their work from a
shared RabbitMQ queue, so the same item is never processed twice regardless of how many instances happen to poll at
once. Set the `DISABLE_CRON` environment variable to `true` on an instance to opt it out, e.g. to run `cron` on a
different schedule yourself instead.

## File storage buckets

Ember Nexus stores uploaded files in two separate S3-compatible buckets: `file.S3UploadBucket` holds chunks of
resumable uploads still in progress, and `file.S3StorageBucket` holds files that have been fully attached to an
element - see [Uploads operate on temporary storage](/reference/upload#chunk-storage) for how a file moves from one
to the other. Because nothing in the upload bucket is meant to outlive its upload, and Ember Nexus already cleans up
expired uploads itself (see [Expiry](/reference/upload#expiry) in the upload reference and the
[background tasks](#background-tasks) above), operators can safely add a short S3 lifecycle/expiration policy to the
upload bucket as an extra backstop. The storage bucket holds permanently retained files and should not have such a
policy applied to it.

## YML configuration

import config from "../../../../public/ember_nexus.yaml?raw"

The configuration file must be mounted at `/var/www/html/config/packages/ember_nexus.yaml`.

**Note**: The file extension must be `.yaml`.

<Code code={config} lang="yaml" />

This file can be <Link link={{ url: '/ember_nexus.yaml', name: 'downloaded directly' }} download />.
