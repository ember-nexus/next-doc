---
type: reference
title: Cypher Path Subset Search Step
---

The Cypher Path Subset search step is a single task, executable within [search requests](/api/post-search).

It executes a user defined Cypher query against the internal Neo4j database.

## Security aspects

The following safeguards are in place:

- Queries are executed in a **readonly transaction**, which limits access to the database dramatically.
- The **result of queries is validated and filtered** by non-user-defined queries, therefore making sure that no
  inaccessible elements are accessed.
- The **result of the search step is limited** to a list of paths, containing the filtered and validated `elementIds`.
  This makes sure that no manipulated or specifically crafted properties etc. are returned.
- The **user's query is validated against a stricter subset of Cypher 25, called Cypher Path Subset**. It is limited to
  path searches only, i.e. clauses resulting in write access or side effects like existence clauses and function calls
  are forbidden.  
  The query can only return the variable `paths`, which must be of type [Path](https://neo4j.com/docs/cypher-manual/current/patterns/reference/#path-patterns).

With all the different safeguards in place and full test coverage across all guards, **this search step is considered
safe**.

## Input parameters

Any parameters available are passed as query variables to the database, i.e. as `$parameterName` inside the query
string. Unlike the Elasticsearch Query DSL Mixin step, expressions (`{{...}}`) are not supported here.

## Input query

The query must be of type string, and can only return a single variable named `paths`.

## Step result

List of paths result objects, which contain a list of node ids and relation ids.

- `paths`: List of paths returned by Neo4j. Each object contains the following properties:
  - `nodeIds`: List of element ids (UUID), in path order.
  - `relationIds`: List of element ids (UUID), in path order.

## Examples

- [Find tagged elements](/reference/search/cypher-path-subset/find-tagged-elements)
- [Find nested elements](/reference/search/cypher-path-subset/find-nested-elements)
- [Parameterized cypher search](/reference/search/cypher-path-subset/parameterized-cypher-search)

## Example

### Request

```json
{
  "debug": true,
  "steps": [
    {
      "type": "cypher-path-subset",
      "query": "MATCH path=((:Plant)-[:HAS_TAG]->(:Tag {name: \"blue\"})) RETURN path"
    },
    {
      "type": "element-hydration"
    }
  ]
}
```

```bash
curl \
  -H 'Authorization: Bearer secret-token:PIPeJGUt7c00ENn8a5uDlc' \
  -H 'Content-Type: application/json' \
  --data-binary @payload.json \
  https://api.example.com/search
```

### Response

import simplePathSearch from './assets/simple-path-search.json';
import simplePathSearchHeader from './assets/simple-path-search.txt?raw';

<SearchResponseCard
    responseBody={simplePathSearch}
    responseHeaders={simplePathSearchHeader}
    graphLayout="force"
></SearchResponseCard>
