---
type: guide
title: Writing an Elasticsearch query
---

The [Elasticsearch query](/reference/search/elasticsearch-query-dsl-mixin) step lets you run
[Query DSL](https://www.elastic.co/docs/reference/query-languages/query-dsl) queries against the internal
Elasticsearch database. This guide walks through building and running a query; see the
[reference page](/reference/search/elasticsearch-query-dsl-mixin) for the full list of supported features and security
constraints.

## With access to Kibana

If you have access to the Elasticsearch database and Kibana, then you can use [Kibana's console](https://www.elastic.co/docs/explore-analyze/query-filter/tools/console)
to experiment and test custom queries, as well as explore the data schemas of Elasticsearch.  
Note that **Kibana's console requires a full query**, while Ember Nexus API's [`POST /search`](/api/post-search)
endpoint only accepts the **content of the `query` property**:

import emberNexusQuery from "../../03-reference/03-search/01-elasticsearch-query-dsl-mixin/ember-nexus-query.txt?raw"
import kibanaConsoleQuery from "../../03-reference/03-search/01-elasticsearch-query-dsl-mixin/kibana-console-query.txt?raw"

<TwoColumn>
    <ComparisonCard title="Ember Nexus">
        <Code code={emberNexusQuery} lang="bash" />
    </ComparisonCard>
    <ComparisonCard title="Kibana's console">
        <Code code={kibanaConsoleQuery} lang="txt" />
    </ComparisonCard>
</TwoColumn>

## Without access to Kibana

If you do not have direct access, you need to build the query blindly. As small errors can break the whole query, it is
recommended to slowly build your query up, and to test it frequently.

As the base of your query you can use one of the provided [examples](/reference/search/elasticsearch-query-dsl-mixin#examples).

Generative AI like Claude and its competitors are usually experienced enough to help with Elasticsearch query
generation.

## Parameterized Elasticsearch queries

Elasticsearch on its own does not support parameters in their Query DSL language. However, Ember Nexus uses the
[Expr Lang](https://github.com/expr-lang/expr) library to integrate expressions directly into the Query DSL query.  
These expressions need to be defined as strings surrounded by two curly brackets, e.g. `{{your_expression}}`, and their
result will replace the expression string inside the query. The replaced value can be a string itself, but also all
other JSON data types like numbers, arrays and objects are supported.  
Expressions will receive the search step's parameters as their only input.

You can read the [Expr library's documentation to explore their features](https://expr-lang.org/docs/language-definition)
and use their [playground to test your own queries](https://expr-lang.org/playground). See also
[parameters and expressions](/guide/search/parameters-and-expressions) for how parameters flow between search steps.

Example:

```json /{{\w+}}/
{
  "debug": true,
  "steps": [
    {
      "type": "elasticsearch-query-dsl-mixin",
      "query": {
        "terms": {
          "_id": "{{someElementIds}}"
        }
      },
      "parameters": {
        "someElementIds": [
          "258c0dfe-b1d8-4839-beed-d00d1b544a96",
          "23508e6c-632a-496d-9bd6-8c62a03216af",
          "8940d70b-5b6f-43b7-bee4-41d073396ff8"
        ]
      }
    },
    {
      "type": "element-hydration"
    }
  ]
}
```
